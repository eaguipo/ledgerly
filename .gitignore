# Secrets — never commit
.env
.env.local
.env*.local

# Node / Next.js (once the app is scaffolded inside this repo)
node_modules/
.next/
out/
dist/
build/

# Supabase CLI (if used later)
.supabase/

# Infra: local Helm overrides (may hold Supabase creds) + vendored subcharts
deploy/helm/**/values.local.yaml
deploy/helm/**/charts/*.tgz
deploy/helm/**/Chart.lock

# Kubeconfig dumps
kubeconfig*.yaml

# Logs & OS cruft
*.log
.DS_Store

# Intermediate design artifact
.design-meta.json
